# Roast session log

<!-- Maintained by /twt-roast-full and /twt-roast-express. One "## Run" section is appended per
     invocation (newest at the bottom); never rewrite earlier runs. Fill the timeline live as the
     run proceeds — one entry per question asked and per skill dispatched. -->

## Run <ISO timestamp>

- **Command:** </twt-roast-full | /twt-roast-express>
- **Mode:** <interactive | auto>
- **Target:** <Static HTML | Elementor | Figma express | (tbd)>
- **Requested:** <one line — the user's free-form context / starting state at invocation>

### Timeline
<!-- Number entries in order. Each entry is either a QUESTION (record the answer) or a STEP
     (record the skill used + a one-sentence why). Keep "why" to a single sentence. -->
1. **[question] <header>** — asked: "<question text>" → **answer:** <user's choice / "You decide" / auto-decision: <value>>.
2. **[step] <phase or action>** — skill: `</twt-...>` — why: <one sentence of context>.
3. **[question] <header>** — asked: "<question text>" → **answer:** <…>.
4. **[step] <…>** — skill: `</twt-...>` — why: <…>.
<!-- In auto mode there are no user answers: record each inferred choice as "auto-decision: <value> (from <evidence|default>)". -->

### Outcome
- **Phases / steps completed:** <list>
- **Outstanding BLOCKERs:** <count + where, or none>
- **Key artifacts:** <paths — pre-design-brief.md, design-brief.md, site/ or theme, qa-report.md, …>
